import 'package:ecommerce_app/src/features/authentication/domain/app_user.dart';

/// A record type that represents the last orderId for a given user
typedef UserOrderID = ({UserID uid, String? orderId});
