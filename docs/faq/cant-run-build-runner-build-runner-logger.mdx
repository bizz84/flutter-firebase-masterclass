# Can't run build_runner because BuildForInputLogger

This issue may be encountered when running `dart run build_runner watch -d`:

- [Can't run build_runner because BuildForInputLogger](https://github.com/dart-lang/build/issues/3519)

The suggested solution is to:

- upgrade to Flutter 3.10 or above
- run `flutter pub upgrade`
